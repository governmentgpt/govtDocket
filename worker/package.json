{
  "name": "wikigov-api",
  "version": "1.0.0",
  "description": "WikiGov RAG retrieval API — Cloudflare Worker with Hono",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:supabase-url": "wrangler secret put SUPABASE_URL",
    "secret:supabase-key": "wrangler secret put SUPABASE_ANON_KEY",
    "secret:nvidia-key": "wrangler secret put NVIDIA_API_KEY"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.65.0"
  }
}
